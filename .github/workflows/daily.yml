name: BullTrend Scout Daily
on:
  schedule:
    - cron: '30 16 * * *'
  workflow_dispatch:
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: pip install yfinance pandas ta requests
      - name: Run analysis
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.8480788421:AAHslBL99YVihLzv8XzsO_gxodOwmvAzKbg}}
          TELEGRAM_CHAT_ID: ${{ secrets.732005110}}
        run: python run_daily.py
